# LiveDeck Releases

Official public Windows release channel for LiveDeck.

Use the GitHub **Releases** page for `LiveDeck-Setup.exe`, signed `latest.json`, checksums, and release notes.

This repository is intentionally release-only. The LiveDeck source code and update signing private key are not published here.
