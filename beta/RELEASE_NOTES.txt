LiveDeck v0.9.0-beta.3

- Updater実機ライフサイクル確認専用のBetaです。
- v0.9.0-beta.2から機能追加はありません。
- beta.2で導入した physical file / Temp staging / base preflight / rollback 経路をそのまま使用します。
- beta.1またはbeta.2から、署名・SHA-256・size検証済みのdeltaでbeta.3へ到達できます。
- Stable v0.8.14は変更していません。

このBetaの目的は、実Installed環境で 検出 -> 取得 -> staging -> 終了 -> swap -> 再起動 を確認することです。
